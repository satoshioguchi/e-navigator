<%= form_for @interview, url: user_interview_path do |f| %>

<h1>面接編集</h1>

  <div class="field">
    <%= f.label :開始時間 %><br />
    <%= f.datetime_select(
        :schedule
        ) %>
  </div>
  
  <div class="actions">
    <%= f.submit "登録" %>
  </div>
<% end %>
