<h1><%= @user.username %>さんの面接一覧</h1>
<h2>現在の面接日程</h2>
<% unless @approval_interview.nil? %>
  <p><%= @approval_interview.schedule.strftime("%Y年%m月%d日 %H時%M分") %>に面接が設定されています</p>
<% end %>
<p>面接日程を変更する場合は以下から選んでください</p>
<table>
  <tbody>
    <% @interviews.each do |interview| %>
    <tr>
      <td>
        <%= interview.schedule.strftime("%Y年%m月%d日 %H時%M分") %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
