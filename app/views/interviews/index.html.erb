<h1><%= @user.username %>さんの面接一覧</h1>
  <table>
    <theader>
      <tr>
        <th>面接開始時間</th><th>承認状態</th><th></th><th></th>
      </tr>
    </theader>
    <tbody>
      <% @interviews.each do |interview| %>
      <tr>
        <td>
          <%= interview.schedule.strftime("%Y年%m月%d日 %H時%M分") %>
        </td>
        <td>
          <%= interview.propriety %>
        </td>
        <% if @user == current_user %>
          <td class="btn-top-radius">
            <%= link_to '編集', edit_interview_path(current_user) %>
          </td>
          <td class="btn-top-radius">
            <%= link_to '削除', '#' %>
          </td>
        <% end %>
      </tr>
      <% end %>
    </tbody>
  </table>
  <div class="btn-sf-like">
  <% if @user == current_user %>
    <%= link_to '新規面接作成', new_interview_path %>
  <% end %>
  </div>
